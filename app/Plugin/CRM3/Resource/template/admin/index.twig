{#
#}
{#
[お問い合わせ管理]-[お問い合わせ一覧]画面
#}

{% extends 'default_frame.twig' %}

{% set menus = ['admin_crm3', 'admin_crm3'] %}

{% block title %}お問い合わせ管理管理{% endblock %}
{% block sub_title %}お問い合わせ一覧{% endblock %}

{% block stylesheet %}
{% endblock stylesheet %}

{% block javascript %}
{% endblock javascript %}

{% block main %}
<form name="search_form" id="search_form" method="post" action="">
    <div class="row">
        <div class="col-md-12">
            <div class="box">
                <div class="box-header with-arrow">
                    <h3 class="box-title"><span class="normal"><strong>{{ totalItemCount }} 件</strong> が該当しました</span></h3>
                </div><!-- /.box-header -->
                <div class="box-body">
                    <div class="table_list">
                        <div class="table-responsive with-border">
                            <table class="table table-striped">
                                <col width="15%">
                                <col width="15%">
                                <col width="70%">
                                <thead>
                                <tr>
                                    <th>お問い合わせ日時</th>
                                    <th>名前</th>
                                    <th>内容</th>
                                    <!--
                                    <th>編集</th>
                                    <th>削除</th>
                                    -->
                                </tr>
                                </thead>
                                <tbody>
                                {% for item in pagination %}
                                <tr>
                                    <td class="">{{ item.createDate|date('Y-m-d H:i:s') }}</td>
                                    <td class="">
                                        {{ item.name }}<br>
                                        {{ item.email }}
                                    </td>
                                    <td class="">{{ item.contents|nl2br }}</td>
                                    <!--
                                    <td><a href="{{ url('admin_crm', { id : item.contactId }) }}" >編集</a></td>
                                    <td><a href="{{ url('admin_crm', {'id' : item.contactId}) }}" {{ csrf_token_for_anchor() }} data-method="delete" data-message="おすすめ情報を削除してもよろしいですか？">削除</a></td>
                                    -->
                               </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div><!-- /.box-body -->
            </div><!-- /.box -->
        </div><!-- /.col -->
    </div>
</form>
{% endblock %}
